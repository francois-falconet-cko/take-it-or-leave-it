node_modules/
.next/
*.tsbuildinfo
.env
.env.local

# Real rates never leave the machine by accident. The demo book is committed;
# the live one is not. Re-create it with `npm run book:compile`.
data/pricing-book.json
sources/acq framework.csv
sources/*.eml
sources/raw/*.txt
# Converted Highspot product pricing decks and the MAC, plus the hand
# transcription of their rate tables. Same reason as the CSV: real rates.
pricing-files/
pricing-book.diff.md
.DS_Store
out/
dist/
